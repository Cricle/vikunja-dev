# Vikunja Webhook Notification System - Web UI

基于 Vue3 + TypeScript + Vuestic 的 Vikunja Webhook 通知系统 Web 界面。

## 功能特性

- 📝 **Dashboard** - 配置概览和快速操作
- ⚙️ **Provider Configuration** - 配置通知提供者（PushDeer 等）
- 📁 **Project Rules** - 为不同项目配置不同的通知规则
- ✏️ **Template Editor** - 自定义通知模板，支持占位符

## 技术栈

- **Vue 3** - 使用 Composition API
- **TypeScript** - 类型安全
- **Vuestic UI** - 美观的 UI 组件库
- **Pinia** - 状态管理
- **Vue Router** - 路由管理
- **Axios** - HTTP 客户端
- **Vite** - 快速的构建工具

## 安装

```bash
npm install
```

## 开发

```bash
npm run dev
```

开发服务器将在 http://localhost:5173 启动。

API 请求会自动代理到后端服务器（默认 http://localhost:5000）。

## 构建

```bash
npm run build
```

构建产物将输出到 `dist` 目录。

## 项目结构

```
src/
├── views/              # 页面组件
│   ├── Dashboard.vue
│   ├── ProviderConfig.vue
│   ├── ProjectRules.vue
│   └── TemplateEditor.vue
├── components/         # 可复用组件
├── stores/            # Pinia 状态管理
│   ├── configStore.ts
│   └── eventStore.ts
├── services/          # API 服务
│   └── api.ts
├── types/             # TypeScript 类型定义
│   ├── config.ts
│   └── events.ts
├── router/            # 路由配置
│   └── index.ts
├── App.vue            # 根组件
└── main.ts            # 入口文件
```

## 使用指南

### 1. 配置提供者

在 **Providers** 页面：
1. 选择提供者类型（如 PushDeer）
2. 输入 API 密钥
3. 点击 "Test Notification" 测试连接
4. 保存配置

### 2. 配置项目规则

在 **Project Rules** 页面：
1. 为特定项目或所有项目配置规则
2. 选择要启用的事件类型
3. 保存配置

### 3. 自定义模板

在 **Templates** 页面：
1. 选择事件类型
2. 编辑标题和正文模板
3. 使用占位符（如 `{{task.title}}`）
4. 查看右侧的占位符参考
5. 保存模板

### 可用占位符

#### 任务事件
- `{{task.title}}` - 任务标题
- `{{task.description}}` - 任务描述
- `{{task.id}}` - 任务 ID
- `{{task.done}}` - 任务完成状态
- `{{project.title}}` - 项目标题
- `{{assignees}}` - 分配人列表
- `{{labels}}` - 标签列表

#### 项目事件
- `{{project.title}}` - 项目标题
- `{{project.id}}` - 项目 ID
- `{{project.description}}` - 项目描述

#### 团队事件
- `{{user.name}}` - 用户名称
- `{{user.username}}` - 用户名
- `{{user.email}}` - 用户邮箱

#### 通用
- `{{event.type}}` - 事件类型
- `{{event.timestamp}}` - 事件时间戳
- `{{event.url}}` - 事件 URL

## 设计理念

本 UI 遵循以下设计原则：

1. **VSCode 风格** - 采用 VSCode 官网的视觉设计语言
   - 清晰现代的界面
   - 深色/浅色主题支持
   - 流畅的动画和过渡

2. **Grafana 配置模式** - 采用 Grafana 风格的配置方式
   - 左侧导航栏
   - 主面板用于配置表单
   - 右侧面板用于预览/帮助

## API 端点

Web UI 与以下后端 API 端点交互：

- `GET /api/webhook-config/{userId}` - 获取用户配置
- `PUT /api/webhook-config/{userId}` - 更新用户配置
- `POST /api/webhook-config/{userId}/test` - 测试通知
- `POST /api/webhook` - 接收 webhook 事件

## 开发注意事项

- 所有组件使用 TypeScript 编写
- 使用 Composition API 而非 Options API
- 状态管理使用 Pinia
- 遵循 Vue 3 最佳实践
- 确保类型安全
